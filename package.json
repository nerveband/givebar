{
  "name": "givebar",
  "version": "1.0.0",
  "description": "The open, bulletproof live fundraising thermometer & stage presentation HUD for high-stakes nonprofit galas and benefit auctions.",
  "main": "server/src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run server/src/index.ts",
    "dev": "bun --watch server/src/index.ts",
    "test": "bun test",
    "build:static": "mkdir -p dist-static/css dist-static/js && cp -r client/public/*.html dist-static/ && cp -r client/css/*.css dist-static/css/ && cp -r client/js/*.js dist-static/js/"
  },
  "keywords": [
    "fundraising",
    "gala",
    "thermometer",
    "stage-hud",
    "live-appeal",
    "pledge-tracker",
    "nonprofit"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest"
  }
}
